# Core data + ML stack
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scikit-learn>=1.3,<2.0
scipy>=1.10,<2.0

# Plotting (for calibration figures)
matplotlib>=3.7,<4.0

# Excel / spreadsheet I/O (some historical GMPP files are .xlsx)
openpyxl>=3.1,<4.0
odfpy>=1.4,<2.0

# Web scraping for the gov.uk download script
requests>=2.31,<3.0
beautifulsoup4>=4.12,<5.0

# Optional gradient-boosting backend (sklearn HistGradientBoosting is
# the fallback if this is not installed)
lightgbm>=4.0,<5.0


